% \begin{tikzpicture}[
%   node style/.style={draw, rounded corners, minimum width=2.5cm, minimum height=1cm, align=center},
%   -{Stealth}, thick
%   ]

%   % Definição das caixas
%   \node[node style] (renda) at (0, 2) {Renda};
%   \node[node style] (poupa) at (3, 0) {Poupa};
%   \node[node style] (investe) at (0, -2) {Investe};
%   \node[node style] (retorno) at (-3, 0) {Retorno};

%   % Setas com textos alinhados à direção das setas
%   \draw (renda) -- node[above] {} (poupa);
%   \draw (poupa) -- node[right] {} (investe);
%   \draw (investe) -- node[below] {} (retorno);
%   \draw (retorno) -- node[left] {} (renda);

% \end{tikzpicture}

\begin{tikzpicture}[
    node style/.style={draw, rounded corners, minimum width=2.5cm, minimum height=1cm, align=center},
    node distance=2cm % Adiciona espaçamento entre os nós
  ]

  % Definição das caixas
  \node[node style] (renda) {Renda};
  \node[node style, right=of renda] (poupa) {Poupa};
  \node[node style, below=of poupa] (investe) {Investe};
  \node[node style, left=of investe] (retorno) {Retorno};

  % Setas com textos alinhados à direção das setas
  \draw[->, thick, -{Stealth}, shorten >=3pt, shorten <=3pt] (renda) -- node[above] {Transfere} (poupa);
  \draw[->, thick, -{Stealth}, shorten >=3pt, shorten <=3pt] (poupa) -- node[right] {Aplica} (investe);
  \draw[->, thick, -{Stealth}, shorten >=3pt, shorten <=3pt] (investe) -- node[below] {Retorna} (retorno);
  \draw[->, thick, -{Stealth}, shorten >=3pt, shorten <=3pt] (retorno) -- node[left] {Reinveste} (renda);

\end{tikzpicture}

